import Header from '@/components/Header';
import Layout from '@/components/Layout';

const ListPage = () => {
  return (
    <Layout>
      <h1>List</h1>
    </Layout>
  );
};

export default ListPage;
